import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    heading1: {
        fontSize: 24,
        fontWeight: 'bold'
    },
    primaryColor:{
        color: '#1ed1df'
    }
})

const fontsStyle = StyleSheet.create({
    font18:{fontSize: 18}
})

export  {globalStyles, fontsStyle};